# Analytics Core Base Module
This module is the base module for Analytics Core. It contains the common utilities, the base traits and structs for the Analytics Core.

## Crate Features
- `python-binding`: Enable the Python binding to access the Manifest struct in Python. This feature is disabled by default.